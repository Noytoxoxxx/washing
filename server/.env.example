DATABASE_URL="postgresql://postgres:postgres@localhost:5432/veyza"
JWT_SECRET="change-me-in-production-to-a-long-random-string"
PORT=4000
CLIENT_URL="http://localhost:5173"
NODE_ENV="development"

# Production only (e.g. Render persistent disk mount) — omit for local dev, where
# uploads default to server/uploads.
# UPLOAD_DIR="/var/data/uploads"
